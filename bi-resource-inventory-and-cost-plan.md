# BI 项目资源盘点与降本方案（基于账单）

> **口径**：阿里云账单税前金额 `PretaxAmount`，按实例/计费项汇总。  
> **覆盖**：集群内（ACK 节点/SLB/日志）+ 集群外（ECS、EMR、RDS、Redis、Kafka、Flink、OSS、EIP、VPN 等）。  
> **环境标签**：`BI-diangao-ali`（主生产）/ `biguonei-diangao-ali`（国内）/ `bitest-diangao-ali`（测试）/ `bidb-diangao-ali`（库）/ `binew-diangao-ali` / `BI-daingao-ali`（拼写不同，疑似错挂）。  
> **本单合计（税前）**：约 **¥15.31 万**。目标区间 15%–30% 对应月节约 **¥2.3 万 – ¥4.6 万**。

---

## 0. 结论先行

成本不是「机器多而散」，而是 **少数几条生产线把账单撑起来了**：

| 排名 | 成本单元 | 约 ¥ | 占总账单 | 判断 |
|------|----------|------|----------|------|
| 1 | EMR 计算集群 `c-211cbbee7a09d77e` | 38,282 | 25.0% | 计算 CU 过大，优先压 CU |
| 2 | EMR 存储集群 `c-70afd013dd6a6a8e` | 33,308 | 21.8% | 存储费接近计算费，冷数据/生命周期必须动 |
| 3 | ACK 主集群 `cd9183fe…`（美东 Worker） | 28,182 | 18.4% | 节点+系统盘是第二大计算账单 |
| 4 | OSS 美东标准存储 | 10,228 | 6.7% | 容量费为主，几乎没有流出，适合沉降级 |
| 5 | EMR 测试集群 `c-9ed416c4aaea232a` | 4,721 | 3.1% | 测试环境常开，P0 可砍 |
| 6 | 美东 EIP 出方向流量 | 4,685 | 3.1% | 流量出口集中，先查是谁在跑公网 |
| 7 | Flink `f-cn-bcd40rxz408` | 4,589 | 3.0% | CU 权利 |
| 8 | 公网 SLB 规格虚高（3 台 slb 规格各约 1,037） | 3,110+ | ~2.0% | 明显超配，可降规格 |
| 9 | Kafka `alikafka_post-cn-tk23zpi8y004` | 2,951 | 1.9% | 费用几乎全在「流量规格」 |
| 10 | VPN `vpn-0xi9vyf11nyj5vmd6prh3` | 2,905 | 1.9% | 带宽规格 2,325，偏贵 |

**一句话**：先动 EMR（计算+存储）和 ACK 主集群，再动 OSS/EIP/超配 SLB/测试 EMR。这五块做对，15% 目标就站住；EMR 存储再挖一层，30% 才有可能。

---

## 1. 成本结构

### 1.1 按产品

| 产品 | 约 ¥ | 占比 | 评测要点 |
|------|------|------|----------|
| E-MapReduce | 76,310 | 51% | **绝对主因**。计算集群 + 存储集群 + 测试集群 |
| 云服务器 ECS | 37,548 | 25% | **92% 是五套 ACK Worker**（¥34,409）；其余 ¥3,139 才是独立机，见 §1.4 / §2.14 |
| 对象存储 OSS | 10,255 | 7% | 美东标准存储容量 |
| 云数据传输 CDT | 5,712 | 4% | 几乎全是美东 EIP / SLB 公网流出 |
| 实时计算 Flink | 4,589 | 3% | 计算 CU |
| Tair/Redis | 3,204 | 2% | `dataeye_bi_realtime` 最贵 |
| Kafka | 3,040 | 2% | 流量规格，不是磁盘 |
| VPN 网关 | 2,905 | 2% | 带宽规格 |
| 块存储 | 1,575 | 1% | **不是系统盘**（系统盘在 ECS 里）。这里是额外云盘：北京一块大盘 ¥714 + 新加坡约 147 块 40G 小盘 ¥787，见 §2.15 |
| RDS | 1,036 | 1% | 3 套库，体量可控 |
| ACK 管理费 | 762 | 0.5% | 至少 4 套 Pro 集群在付钱 |
| 负载均衡 | ~5,170 | ~3.4% | 含漏计的一批公网 CLB；3 台规格费异常高 |
| 其他（证书、NAT、SLS、EIP 保有） | <1,000 | <1% | 证书通配符 ¥569 可单独看 |

### 1.2 按项目标签

| 标签 | 约 ¥ | 占比 | 含义 |
|------|------|------|------|
| BI-diangao-ali | 13.6 万+ | ~92% | 主账单，降本主战场 |
| bitest-diangao-ali | 4,743 | 3.1% | **测试 EMR 几乎独占** |
| biguonei-diangao-ali | 5,013 | 3.3% | 国内 ACK + RDS/Redis/NAT |
| BI-daingao-ali | 1,154 | 0.8% | 拼写与主标签不一致，Jira/财务/hk 机挂在这里 |
| bidb / binew | ~1,300 | <1% | 库与新账号，体量小 |

### 1.3 ACK 集群对照（节点费 + ACK Pro）

账单里 Worker 行按集群 nickname 汇总，**把它当集群总成本，不要当成单机**。

| 集群 ID | 角色判断 | 节点相关 ¥ | ACK Pro ¥ | 合计 |
|---------|----------|------------|-----------|------|
| `cd9183fe75e5441a79fefd718771bd15b` | 美东主集群 | 27,801 | 381 | **28,182** |
| `cf434eaf1f71c400b8e2dd3936f46a452` | 国内集群 | 3,258 | 190 | **3,449** |
| `c04c80d7957e54576bdc38c192d2ffe64` | 美东另一套（账号 1893…） | 2,359 | 190 | **2,550** |
| `ca171f34e7d734227865cadb1e2d710a6` | 新加坡小集群 | 630 | — | 630 |
| `c38cd38a4923740ddb88c922ecca1812d` | 美东小集群 | 360 | — | 360 |

五套 ACK 同时付 Pro 管理费，且 Worker 规格/磁盘明显不均。主集群系统盘一项就 ¥6,190，说明盘不是「40G 系统盘」这个量级。

### 1.4 ECS ¥37,548 拆开看

先分清两笔账，不要混着说「机器贵」：

| 类别 | 约 ¥ | 占 ECS | 是什么 |
|------|------|--------|--------|
| ACK Worker（5 套集群） | **34,409** | **92%** | K8s 节点的「云服务器配置 + 系统盘」，按集群 nickname 汇总 |
| 独立 ECS（非 Worker） | **3,139** | **8%** | 有自己主机名的虚拟机，和节点池无关 |

Worker 再按集群拆（只含 ECS，不含 ACK Pro 管理费）：

| 集群 | 配置/实例 | 系统盘 | ECS 小计 | 怎么读 |
|------|-----------|--------|----------|--------|
| 美东主集群 `cd9183fe…` | 21,611 | 6,190 | **27,801** | 节点算力 + **异常大的系统盘** |
| 国内 `cf434eaf…` | 3,078 | 180 | 3,258 | 正常量级 |
| 美东第二套 `c04c80d7…` | 2,214 | 145 | 2,359 | 另一个账号上的一套 |
| 新加坡 `ca171f34…` | 604 | 26 | 630 | 小集群 |
| 美东小集群 `c38cd38a…` | 336 | 24 | 360 | 小集群 |

独立机再按用途拆（明细见 §2.14）：

| 用途 | 约 ¥ | 机器 |
|------|------|------|
| 疑似业务机 | 1,024 | `ali-iad105-BI-*`、`dataeyebi-as-*`、`biplatform-35-11` |
| 公网流量异常 | 661 | `ali-Zmaticoo-IAD`（其中流出 **¥576**） |
| 网络件 vCPE | 579 | `vcpe` ×2 地域 + `ali-sin138-47-206-bi-vCPE` |
| 疑似非 BI / 错挂 | 447 | `jira`、`财务系统new`、`abby_order_system`、`hk1/hk2` |
| 临时机 | 419 | `launch-advisor-20260206/09` |
| 备案 | 10 | `bi-domestic-beian` |

**读法**：ECS 的降本主战场是美东主集群 Worker（尤其系统盘 ¥6,190），不是去关 Jira。独立机里能立刻动的是 launch-advisor 和错挂标签。

---

## 2. 逐项评测

每一项按同一张卡写：**现状 → 问题 → 建议动作 → 预估月节约 → 风险**。

---

### 2.1 EMR 计算 `c-211cbbee7a09d77e`　¥38,282

| 字段 | 内容 |
|------|------|
| 形态 | EMR CU 34,300 + 内存增强型 CU（包月）3,982 |
| 环境 | 生产 `BI-diangao-ali` |
| 问题 | 账单一半在这一套计算上。包月内存增强 CU 说明有保底，按量 CU 才是弹性浪费来源 |
| 建议 | ① 拉近 14 天 CU 利用率/队列等待 ② 按作业波峰改弹性、低谷缩 CU ③ 确认包月 CU 是否买超 ④ 能下线的作业合并 |
| 预估节约 | 保守砍 20% 按量 CU ≈ **¥6,900**；激进 30% ≈ **¥10,300**（不动包月先） |
| 风险 | 中。作业超时、报表延迟。先 staging/非高峰验证 |
| 优先级 | **P1**（金额最大，需要利用率证据） |

---

### 2.2 EMR 存储 `c-70afd013dd6a6a8e`　¥33,308

| 字段 | 内容 |
|------|------|
| 形态 | 存储大小 31,890 + 全托管存储 1,418 |
| 问题 | 存储费几乎追平计算费。BI 数仓典型浪费：热数据盘扛着冷分区、无生命周期 |
| 建议 | ① 表/分区级看容量 Top ② 超 N 天数据沉 OSS 低频/归档 ③ 删临时表、无主表 ④ 压缩与小文件合并 |
| 预估节约 | 冷数据沉降 15% ≈ **¥4,800**；做到 25% ≈ **¥8,000** |
| 风险 | 中。误沉影响即席查询。先只动明确冷分区 |
| 优先级 | **P1** |

---

### 2.3 EMR 测试 `c-9ed416c4aaea232a`　¥4,721

| 字段 | 内容 |
|------|------|
| 形态 | CU 4,480 + 存储 241，标签 **bitest** |
| 问题 | 测试集群按「小生产」在烧。CU 是主因 |
| 建议 | ① 非工作时段关机/缩到最小 CU ② 能共用生产只读则停计算 ③ 存储 PL0 再确认是否必要 |
| 预估节约 | 关一半时间或缩 60% CU ≈ **¥2,700–¥3,800** |
| 风险 | 低。测试可回滚 |
| 优先级 | **P0** |

---

### 2.4 ACK 主集群 `cd9183fe…`　¥28,182

| 字段 | 内容 |
|------|------|
| 形态 | Worker 配置 21,611 + 系统盘 6,190 + ACK Pro 381 |
| 地域 | 美东（实例前缀 `i-0xi`） |
| 问题 | BI/业务 K8s 主战场。系统盘 6,190 不正常（不是标准 40G 系统盘）；节点规格偏大或节点过多 |
| 建议 | ① 导出节点数、规格、request/limit、实际 CPU/内存 ② 缩空节点、降规格、拆大盘 ③ 确认是否混部了非 BI 负载 ④ 系统盘改云盘并限制 Docker 日志 |
| 预估节约 | 节点 20% ≈ **¥5,600**；系统盘治理 30% ≈ **¥1,900**；合计约 **¥7,500** |
| 风险 | 中高。生产。必须有监控证据 + 回滚 |
| 优先级 | **P1** |

---

### 2.5 ACK 国内 `cf434eaf…`　¥3,449　/　美东第二套 `c04c80d7…`　¥2,550　/　新加坡 `ca171f34…`　¥630　/　小集群 `c38cd38a…`　¥360

| 字段 | 内容 |
|------|------|
| 问题 | **五套 ACK 都在付 Pro**。国内、美东两套、新加坡、再加一套小集群，管理面重复 |
| 建议 | ① 写清每套集群业务 ② 能合并的并（尤其两套美东）③ 无业务的直接缩到 0 并删集群停 Pro |
| 预估节约 | 合并/下线 1 套 Pro + 对应节点，低估计 **¥500–¥2,500**（看哪套是空的） |
| 风险 | 中。要先认集群用途 |
| 优先级 | **P2**（先盘点归属，再合并） |

---

### 2.6 OSS 美东标准存储　¥10,228

| 字段 | 内容 |
|------|------|
| 形态 | `us-east-1;standard` 容量 10,164；请求/流出只有几十元 |
| 问题 | **容量费，不是流量费**。数据躺着烧钱，几乎没人读外网 |
| 建议 | ① 生命周期：30/90 天转低频/归档 ② 删过期导出、重复副本 ③ 确认是否备份了 EMR/日志 |
| 预估节约 | 20% 沉降 ≈ **¥2,000**；40% ≈ **¥4,000** |
| 风险 | 低–中。恢复时效变差 |
| 优先级 | **P1** |

---

### 2.7 美东公网流量（CDT）　¥5,712

| 字段 | 内容 |
|------|------|
| 大头 | EIP `eip-0xin4dn4571tnwv8zs1bk` 出方向 **¥4,685** |
| 其次 | SLB `lb-0ximfx84fe244ytqp6cnv` 公网出方向 **¥888** |
| 问题 | 出口集中在 1 个 EIP + 1 个公网 SLB。不是「很多 EIP 漏钱」，是 **一条业务公网路径很贵** |
| 建议 | ① 对应该 EIP/SLB 后面的服务 ② 能走内网/专线/OSS 内网则切走 ③ 压缩、缓存、CDN ④ 确认不是日志/数据同步在走公网 |
| 预估节约 | 压 20% ≈ **¥1,100**；若误走公网可更高 |
| 风险 | 中。改链路要验证 |
| 优先级 | **P1**（先归因，再改） |

---

### 2.8 Flink　¥4,589

| 字段 | 内容 |
|------|------|
| 形态 | 计算资源 4,417 + 管控 172 |
| 建议 | 作业 CU 对账、空作业下线、批作业错峰；管控费随实例走，先砍计算 |
| 预估节约 | 20% CU ≈ **¥880** |
| 风险 | 中。实时链路 |
| 优先级 | **P1** |

---

### 2.9 Kafka　¥3,040

| 字段 | 内容 |
|------|------|
| 形态 | `alikafka_post-cn-tk23zpi8y004`：流量规格 2,635 + 磁盘 312 + 分区 4；另有旧实例磁盘 89 |
| 问题 | 钱在 **流量规格档位**，不在磁盘。规格买高了会整月付钱 |
| 建议 | 看峰值流量是否远低于档位；降档；确认第二实例 `kf74hp9gq001` 是否残留 |
| 预估节约 | 降一档或删残留 ≈ **¥800–¥1,500** |
| 风险 | 中。高峰打满会限流 |
| 优先级 | **P1** |

---

### 2.10 Redis / Tair　¥3,204

| 实例 | ¥ | 评测 |
|------|--|------|
| `dataeye_bi_realtime` | 2,259 | 生产实时，分片规格贵。看 QPS/内存水位再降配 |
| `ag-event-server` + enterprise | 610 | 两套事件相关 Redis，问是否可合并 |
| `bi-zh-redis` | 242 | 国内，体量正常 |
| `dataeye_bi_redis` | 103 | 较小 |
| `dataeye_bi_redis_test` | -10 | 已有退款/释放痕迹，确认是否清干净 |

建议：realtime 降配或改按量需证据；事件双套合并是架构项。  
预估节约：**¥300–¥800**（P1/P2）。

---

### 2.11 VPN　¥2,905

| 字段 | 内容 |
|------|------|
| 形态 | 带宽规格 2,325 + IPsec 580 |
| 问题 | 带宽档位是大头。专线/VPN 买宽了会稳定浪费 |
| 建议 | 看实际带宽 P95；降档；确认是否仍有同等专线可替代 |
| 预估节约 | 降档 20%–30% ≈ **¥500–¥800** |
| 风险 | 中。跨国访问质量 |
| 优先级 | **P1** |

---

### 2.12 公网 SLB 超配　约 ¥3,110 规格费 + 其余实例费

账单后半段有 **3 台 CLB 实例规格各约 ¥1,037**：

- `lb-0xi9ldvjdal4h0l7jyf2a`
- `lb-0xisk0yyuw1u9jzt8zcsr`
- `lb-0xihahk7rjdk5oz49bgns`

其余大量公网 CLB 是「实例费 ≈40 + 规格 ≈33 + IP 保有 ≈5」，约 **¥78/台/月**，台数多但单台不离谱。

| 建议 | 3 台高规格先核对 QPS/连接数，降到与其它 CLB 同档；无业务的公网 CLB 删除 |
| 预估节约 | 3 台降配 ≈ **¥2,800–¥3,000**；再清闲置台数另算 |
| 风险 | 中。降配可能触达规格上限 |
| 优先级 | **P0**（金额清楚、动作标准） |

---

### 2.13 RDS　¥1,036

| 实例 | ¥ | 评测 |
|------|--|------|
| `ag-event-server` | 548 | 规格 472 为主，可看 CPU/连接数 |
| `bi-zh-mysql` | 375 | 国内 |
| `mediabuy-dmp-ali` | 113 | 较小 |

不是主战场。有空再降配。预估 **¥100–¥200**。

---

### 2.14 独立 ECS（非 ACK Worker）　¥3,139

ACK 节点已经在 §1.4 / §2.4 里。这里只说 **有主机名、不叫 `worker-k8s-for-cs-*` 的机器**。一共约 16 台量级，¥3,139，只占 ECS 的 8%。

#### 业务机（先保留，降配要监控）

| 机器 | 约 ¥ | 构成 | 评测 |
|------|------|------|------|
| `ali-iad105-BI-1-137` | 395 | 实例 338 + 数据盘 52 + 系统盘 5 | 美东业务机，带数据盘，像正式服务 |
| `ali-iad105-BI-0-74` | 275 | 实例 269 + 系统盘 6 | 同系列，无大的数据盘 |
| `ali-iad105-dataeyebi-as-1-40` | 137 | 实例 81 + 数据盘 52 + 系统盘 5 | 一对 AS，标签是 `BI-daingao-ali` |
| `ali-iad105-dataeyebi-as-1-41` | 137 | 同上 | 与 40 对称，确认是否还要双活 |
| `ali-iad102-biplatform-35-11` | 79 | 实例 74 + 系统盘 5 | 小，账号 1893… |

#### 公网异常：`ali-Zmaticoo-IAD`　¥661

实例只有 ¥77，系统盘 ¥8，**流出流量 ¥576**。机器本身不贵，贵的是这台在跑公网。先查：是业务出口、同步任务，还是误开公网 IP。流量降下来，这台从「独立机里最贵」变成普通小机。

#### 网络件 vCPE　¥579

| 机器 | 地域 | 约 ¥ | 评测 |
|------|------|------|------|
| `vcpe` `i-0xicvfrz9kltie0htm1a` | 美东 | 241 | 配置 237 + 系统盘 4 |
| `vcpe` `i-2ze75d81ix4era6ak3wd` | 国内 | 56 | 实例 51 + 系统盘 5 |
| `ali-sin138-47-206-bi-vCPE` | 新加坡 | 282 | 配置 274 + 系统盘 7 |

三台都是 CPE/专线侧虚拟设备，不是应用机。降本方式是确认链路是否还在用，而不是缩 CPU。不用了才能关。

#### 临时机　¥419　→ P0 关停

| 机器 | 约 ¥ | 构成 |
|------|------|------|
| `launch-advisor-20260209` | 307 | 实例 203 + 操作系统 94 + 系统盘 11 |
| `launch-advisor-20260206` | 112 | 实例 101 + 系统盘 11 |

名称就是阿里云「实例启动顾问」拉起来的评估机，还挂着 Windows 类操作系统费。创建于 2026-02，账单期仍在计费 = **忘关**。确认控制台无业务后释放。

#### 疑似非 BI / 标签错挂　¥447

全部在账号 `1053564683980631`、标签 **`BI-daingao-ali`**（比主标签少一个 i）：

| 机器 | 约 ¥ | 构成 |
|------|------|------|
| `财务系统new` | 148 | 实例 126 + 系统盘 23 |
| `jira` | 108 | 实例 84 + 系统盘 25 |
| `abby_order_system` | 95 | 实例 87 + 系统盘 8 |
| `hk1` / `hk2` | 各约 48 | 实例 42 + 系统盘 5 + 少量流出 |

这不像 BI 查询/任务链路。先找 Owner：能迁出项目标签则 **BI 账单下降但公司总成本不变**；确认废弃才能关。备案机 `bi-domestic-beian` ¥10 保留。

| 建议 | P0：两台 launch-advisor；错挂先改标签。Zmaticoo 查流量。业务机和 vCPE 不在第一周动 |
| 预估节约 | 关临时机 **¥419**；错挂迁出可使 BI 账单再降约 **¥447**（不计入集团节约）；Zmaticoo 流量另算 |
| 风险 | 临时机低；错挂低；Zmaticoo 中 |
| 优先级 | 临时机 **P0**；其余按归属 |

---

### 2.15 块存储　¥1,575

先把概念分开，否则会和 ACK 系统盘重复计数：

| 账单产品 | 计费项 | 这笔在哪 | 是什么 |
|----------|--------|----------|--------|
| **云服务器 ECS** | 系统盘 | 已计入上面 ECS ¥37,548 | 节点/虚机的系统盘。美东主集群这一项就 ¥6,190 |
| **块存储** | 磁盘容量 / 快照 | **本段 ¥1,575** | 额外挂载的数据盘、ACK PVC/PV、快照 |

块存储本身只占全账单 1%，不是主战场。需要说清楚的是 **构成很畸形**：一半是北京一块盘，一半是新加坡一百多块同价小盘。

#### 构成

| 项 | 约 ¥ | 占比（块存储内） | 地域（实例 ID 前缀） | 判断 |
|------|------|------------------|----------------------|------|
| `d-2ze5p2f29xbzmekfx3ft` 一块 | **714** | 45% | `d-2ze` = **华北 2 北京** | 单盘，无 nickname，标签 `biguonei-diangao-ali`，和国内 ACK 同一项目 |
| 约 147 块单价 **¥5.3568** | **787** | 50% | `d-t4n` = **新加坡** | nickname 全是 `disk-<uuid>`，很像 K8s CSI 卷 |
| 新加坡稍大盘 `d-t4niino41tmse4gqjmkg` | 25 | 2% | 新加坡 | 同一批里的例外 |
| 美东两块 `d-0xi…` | 各 21，合计 41 | 3% | `d-0xi` = 美东，账号 1893… | 跟美东第二套 ACK 同一账号 |
| 快照链 `us-east-1` | 0.5 | — | 美东 | 可忽略 |
| 其它零碎（¥1.1、¥5.0） | 6 | — | 新加坡 | 可忽略 |

#### 北京那块大盘是什么情况

- 账单上 **只有这一块** 达到 ¥714，其它盘都是十几、二十、五块钱。
- 按新加坡 40G 小盘约 ¥5.36 反推：同样单价大约是 **一百多倍容量**，量级在 **1–2 TB**（若盘类型更贵则容量略小，若更便宜则更大）。精确 GB 以控制台「磁盘容量」为准。
- 标签和国内 ACK `cf434eaf…` 一样是 `biguonei-diangao-ali`，**高度可疑是国内集群的数据盘 / 某台 Worker 的超大挂载盘**，不是 147 块那种散装 PV。
- 国内 ACK 的 **系统盘只有 ¥180**（在 ECS 里），所以这块 **不是系统盘重复计费**，是另外挂上去的。

动作：控制台打开该 disk ID，看挂在哪台 ECS、实际用量。用量远小于容量就缩盘或换小盘；若是日志/数据目录无清理，先清再缩。预估：能缩一半则约 **¥350**。

#### 新加坡那堆 40G 小盘是什么情况

- 约 **147 块**，几乎每块都是 **¥5.3568**，说明规格高度一致（按折扣后价格，对得上 **约 40GB ESSD PL0/入门云盘**）。
- nickname 为 `disk-<uuid>`，符合 ACK **数据盘 / PVC 对应的云盘**，不是「jira」这种业务机系统盘。
- 关键矛盾：**新加坡 ACK `ca171f34…` 的 ECS 一共才 ¥630**（小集群，节点很少），挂不上 147 块在用数据盘。
- 因此更像是：节点扩过再缩容后，**云盘没随节点释放**；或大量 PVC 创建后 Pod 没了盘还在。典型「集群缩了，盘还在计费」。

动作：不要 147 次人工点删除。在块存储控制台筛 `ap-southeast-1`、状态 **待挂载/未挂载**，确认无 PV 后再批量释放。跟节点池缩容绑定：以后缩节点必须开「释放数据盘」。在用的 40G 盘随节点走，不要单独删。

预估：若一半是孤儿盘，约 **¥390**；若大部分在用，节约接近 0，应并进新加坡节点池治理。

| 建议 | 北京大盘：看挂载与用量后缩容。新加坡：先清未挂载孤儿盘，再查 PVC |
| 预估节约 | 大盘缩容 **¥200–¥350**；孤儿盘 **¥0–¥390**。合计上限约 ¥700，仍不是主战场 |
| 风险 | 中。误删 PVC 会丢数据。未挂载盘可先快照再删 |
| 优先级 | **P1**（金额不大，但新加坡孤儿盘是明确垃圾信号） |

---

### 2.16 其它

| 项 | ¥ | 建议 |
|------|--|------|
| SSL 通配符 `binew` | 569 | 年费摊月或一次购买，不是月度浪费；确认是否重复买 |
| NAT 两套 | 174 | 保留，金额小 |
| SLS | 121 | 新加坡 k8s-event 存储 62，可降保存天数 |
| EIP 保有费 | <90 | 未绑实例的 EIP 释放 |

---

## 3. 优化方案总表（按浪费金额 × 风险）

| 优先级 | 动作 | 预估月节约 | 证据门槛 | 风险 |
|--------|------|------------|----------|------|
| P0 | 测试 EMR 缩 CU / 定时关机 | 2,700–3,800 | 标签已是 bitest | 低 |
| P0 | 3 台高规格 SLB 降配 | 2,800–3,000 | 控制台连接数/QPS | 中 |
| P0 | 关停 `launch-advisor-20260206/09` | ~420 | 名称+创建时间 | 低 |
| P0 | 清 `BI-daingao-ali` 错挂（Jira/财务/hk） | 不计节约或迁出 | 业务确认 | 低 |
| P1 | EMR 生产计算压 CU 20%–30% | 6,900–10,300 | 利用率 | 中 |
| P1 | EMR 存储冷数据沉降 15%–25% | 4,800–8,000 | 分区年龄 | 中 |
| P1 | ACK 主集群缩节点/治系统盘 20% | ~7,500 | 监控 | 中高 |
| P1 | OSS 生命周期 20%–40% | 2,000–4,000 | 访问热度 | 低 |
| P1 | EIP/SLB 公网流量治理 20% | ~1,100 | 流量归因 | 中 |
| P1 | Kafka 降档 / Flink 压 CU / VPN 降带宽 | 2,200–3,200 | 峰值 | 中 |
| P2 | 合并 ACK / 合并 Redis | 500–2,500 | 架构评审 | 高 |

**保守合计（P0 + P1 下限）≈ ¥3.1 万（约 20%）**  
**进取合计（取区间中上）≈ ¥4.4 万（约 29%）**

对照目标：

- 15%（¥2.3 万）：只做 P0 + EMR CU 或 ACK 其中一块就能到  
- 30%（¥4.6 万）：EMR 计算和存储都要动，且 ACK/OSS 至少做成一档

---

## 4. 建议执行顺序（给组员直接领）

```text
第 1 周（P0，先拿数字）
  1. 测试 EMR 缩容/定时停
  2. 三台 ¥1037 规格 SLB 降配
  3. 关掉两台 launch-advisor
  4. 列出 Jira/财务/hk 归属，能迁出就迁出

第 2–3 周（P1 大数据）
  5. EMR 计算：利用率 → 压 CU
  6. EMR 存储：冷分区 → OSS
  7. ACK 主集群：空节点、request 超配、系统盘

第 3–4 周（P1 存储与网络）
  8. OSS 生命周期
  9. 美东 EIP 流量归因并改链路
  10. Kafka / Flink / VPN 降档

之后（P2）
  11. ACK 集群合并方案评审
```

每做完一项，用账单对应行核对下降幅度；观察窗口生产 ≥ 24h。

---

## 5. 还缺的证据（补了才能把「预估」改成「已确认」）

账单已经能排序。下列材料用来 **防误砍**：

1. EMR 各集群近 14 天 CU / 存储用量  
2. ACK 主集群节点数、规格、CPU/内存水位、系统盘实际占用  
3. 三台高规格 SLB 的 QPS 与连接数  
4. EIP `eip-0xin4dn4571tnwv8zs1bk` 后面挂的是哪套服务  
5. OSS bucket 列表与生命周期是否已开  
6. 五套 ACK 各自业务 Owner（尤其两套美东）  
7. 块存储：北京盘 `d-2ze5p2f29xbzmekfx3ft` 挂载对象与实际用量；新加坡 `d-t4n*` 里有多少「未挂载」  
8. 独立 ECS：`launch-advisor-*` 是否仍有人用；`ali-Zmaticoo-IAD` 公网流量来源

没有 1/2，不要对生产 EMR/ACK 下手；P0 四项可以先做。

---

## 6. 口径说明

- 金额为税前，未含折扣后实付差异；若财务口径是折后，按同一比例缩放目标即可。  
- ACK Worker 行在导出里按集群 nickname 汇总，评估按集群，不要按单 InstanceID 去控制台找「一台 ¥2 万的机器」。  
- 新加坡小盘数量按账单中单价 ¥5.3568 的行估算约 147 块，精确数以块存储控制台为准。  
- 公网 CLB 后半段在第一次汇总时易漏，方案中的 SLB 节约已按完整明细计入。  
- `BI-daingao-ali` 与 `BI-diangao-ali` 不是同一标签，错挂会让 BI 成本虚高。

---

**修订**：2026-08-13，按当前账单明细出具。数字随下一期账单更新。
