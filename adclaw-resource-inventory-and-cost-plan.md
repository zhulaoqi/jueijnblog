# AdClaw / OpenClaw 资源盘点与降本方案（基于账单）

> **口径**：阿里云账单税前金额，按产品 / 实例 / 计费项汇总。  
> **覆盖**：标签 `adclaw-diangao-ali`，账号 `1340062516391428`（导出里写成 `1.34E+15`，是 Excel 科学计数法，不是另一个账号）。  
> **地域**：新加坡 `ap-southeast-1`（实例前缀 `i-t4n` / `t4n`）。  
> **本单合计（税前）**：约 **¥35,715**。目标区间 15%–30% 对应月节约 **¥5,400 – ¥10,700**。

与《BI 项目资源盘点》不是同一张单：BI 单里新加坡集群 `ca171f34…` 只记了约 ¥630。那是别的标签、别的账期或当时的小规模。**本单是 AdClaw 自己的新加坡生产账，主体已经变成这一套 ACK。**

---

## 0. 结论先行

成本不是「产品多而散」，而是 **一套 ACK 把账单撑起来了**：

| 排名 | 成本单元 | 约 ¥ | 占总账单 | 判断 |
|------|----------|------|----------|------|
| 1 | ACK Worker `ca171f34e7d734227865cadb1e2d710a6` | 32,926 | 92.2% | **绝对主因**。94 台整月节点 + 一批弹性扩容 |
| 2 | VPC 流日志 `fl-t4nuu6j1kygvp0lgrm1am` | 1,055 | 3.0% | 单行最大非计算费，可关或采样 |
| 3 | 块存储（额外云盘，不是系统盘） | 633 | 1.8% | 100 块整月 ¥5.36 小盘，像缩容后没释放的 PVC |
| 4 | 负载均衡（23 台 CLB） | 621 | 1.7% | 22 台整月实例费各 ¥27.34，CU 几乎为 0 |
| 5 | NAT `ngw-t4n034npm655nm4b8hdvo` | 218 | 0.6% | 实例费 + CU，金额小但和公网路径有关 |
| 6 | 公网流量 CDT | 97 | 0.3% | EIP 出流量 49 + **161 台 Worker 都有固定公网 IP 流量** |
| 7 | RDS `biopenclaw` + Tair `adclaw` | 125 | 0.4% | 体量正常，不是主战场 |
| 8 | OSS / SLS / EIP 保有 | 42 | 0.1% | 可忽略金额；SLS 和流日志是一套可观测 |

**一句话**：先动 ACK 常驻节点（94 台整月），再动流日志、未挂载小盘、闲置 CLB。这三块做对，15% 就站住；节点再缩一档，30% 才有可能。RDS / Redis / OSS 不用第一周碰。

---

## 1. 成本结构

### 1.1 按产品

| 产品 | 约 ¥ | 行数 | 占比 | 评测要点 |
|------|------|------|------|----------|
| 云服务器 ECS | 32,926 | 322 | 92.2% | **100% 是这一套 ACK Worker**，没有独立机 |
| 专有网络 VPC | 1,055 | 1 | 3.0% | 只有流日志「使用量」一行 |
| 块存储 | 633 | 158 | 1.8% | **不是系统盘**。系统盘已在 ECS 里（¥3,968） |
| 负载均衡 | 621 | 40 | 1.7% | 23 台 CLB，钱在实例费，不在 CU |
| NAT 网关 | 218 | 2 | 0.6% | CU 129 + 实例费 89 |
| 云数据传输 CDT | 97 | 378 | 0.3% | 行数最多，金额不大；说明 Worker 开了公网 IP |
| 云数据库 RDS | 89 | 2 | 0.2% | `rm-t4n810508q0ruz08g` / `biopenclaw` |
| Tair（Redis） | 36 | 2 | 0.1% | `r-t4nk9cenxrnd7drinm` / `adclaw` |
| 对象存储 | 19 | 4 | 0.1% | 新加坡标准存储容量为主 |
| 日志服务 | 15 | 5 | <0.1% | `clawhub-network-flowlog` |
| 弹性公网 IP | 7 | 1 | <0.1% | `eip-t4nrxabd9ully4adn2brk` 保有费 |

### 1.2 按项目标签

| 标签 | 约 ¥ | 含义 |
|------|------|------|
| `adclaw-diangao-ali` | 35,715 | 本单全部行都是这一套，没有错挂第二标签 |

### 1.3 ACK 集群（按 nickname 汇总，不要当单机）

账单里 Worker 行 nickname 全部是：

`worker-k8s-for-cs-ca171f34e7d734227865cadb1e2d710a6`

**把它当集群总成本，不要按 InstanceID 去控制台找「一台 ¥3 万的机器」。**

| 集群 ID | 角色判断 | 节点配置 ¥ | 系统盘 ¥ | ECS 合计 | 本单是否含 ACK Pro |
|---------|----------|------------|----------|----------|-------------------|
| `ca171f34e7d734227865cadb1e2d710a6` | 新加坡 OpenClaw / AdClaw 生产集群 | 28,957 | 3,968 | **32,926** | 本单未见 Pro 管理费行 |

唯一 ECS 实例数 **161**，但不是 161 台整月在跑：

| 形态 | 台数 | ECS 小计 | 怎么读 |
|------|------|----------|--------|
| 整月节点（配置 ¥272.304 + 系统盘 ¥37.32） | **94** | **29,105** | 常驻节点池，月费约 **¥310 / 台** |
| 接近整月（配置 ¥274.26 + 同规格系统盘） | 1 | 312 | 和整月同一档，略多几个小时 |
| 不足月 / 弹性扩出 | **66** | **3,509** | 配置费折合约 **12.3 台整月**，是 HPA / 节点伸缩痕迹 |

折合常驻约 **106 台整月当量**（94 + 12.3）。钱在「94 台一直开着」，不在那 66 台短命节点。

单台整月拆开：

| 计费项 | 月费 ¥ | 含义 |
|--------|--------|------|
| 云服务器配置 | 272.304 | 按量约 **¥0.378 / 小时**，新加坡 4 核档按量的量级 |
| 系统盘 | 37.319 | 同地域 40G 数据盘单价是 ¥5.36；按同价折算，系统盘大约是 **200G+ 量级**（或更高性能盘），不是默认 40G |

### 1.4 ECS ¥32,926 拆开看

先分清两笔账，不要混着说「机器贵」：

| 类别 | 约 ¥ | 占 ECS | 是什么 |
|------|------|--------|--------|
| ACK Worker 配置 | 28,957 | 88% | 节点算力 |
| ACK Worker 系统盘 | 3,968 | 12% | 节点系统盘，已含在 ECS 产品里 |
| 独立 ECS | **0** | 0% | 本单没有 `worker-k8s-for-cs-*` 以外的主机名 |

**读法**：降本主战场就是这一套节点池。没有 Jira、没有 launch-advisor、没有第二套集群可以先关。

---

## 2. 逐项评测

每一项按同一张卡写：**现状 → 问题 → 建议动作 → 预估月节约 → 风险**。

---

### 2.1 ACK Worker `ca171f34…`　¥32,926

| 字段 | 内容 |
|------|------|
| 形态 | 161 台唯一实例；94 台整月 + 1 台近整月 + 66 台短周期。配置 28,957 + 系统盘 3,968 |
| 地域 | 新加坡 `ap-southeast-1` |
| 环境 | 生产 `adclaw-diangao-ali`，nickname 指向同一 ACK |
| 问题 | ① 常驻 94 台是账单的 81%。② 系统盘月费 ¥37，偏大。③ **每一台都产生了「ECS 固定公网 IP 流量费」**，节点开了公网 IP。④ 66 台短命节点说明伸缩在动，但省下来的钱远小于常驻池 |
| 建议 | ① 导出节点池规格、台数、CPU/内存水位、request/limit ② 空节点、超配 request 先砍，目标先压常驻 15%–30% ③ 系统盘查实际占用，限制容器日志，能改 40G/80G 就改 ④ 节点去掉公网 IP，出口走 NAT / Ingress ⑤ 看 HPA 是否把节点池打出噪声扩容 |
| 预估节约 | 常驻缩 15%（约 14 台）≈ **¥4,300**；缩 20%（约 19 台）≈ **¥5,900**；缩 30%（约 28 台）≈ **¥8,700**。系统盘若从「200G+」收到 80G，94 台再加约 **¥1,000–¥1,800**。弹性噪声压 30% 约 **¥1,000** |
| 风险 | 中高。生产集群。必须有监控 + 回滚；先动空节点和超配，不要先降整池规格 |
| 优先级 | **P1**（金额最大，需要利用率证据）。公网 IP 剥离可按安全项提前做 |

整月节点单台约 ¥310。每关掉 **1 台常驻空节点**，月账单就少约 ¥310，动作比优化 RDS 清晰。

---

### 2.2 VPC 流日志　¥1,055

| 字段 | 内容 |
|------|------|
| 形态 | `fl-t4nuu6j1kygvp0lgrm1am`，「使用量」一行 ¥1,055 |
| 关联 | SLS Project `clawhub-network-flowlog` 另计 ¥15（外网读 7.5 + 存储 3.6 + 读写流量 3.6） |
| 问题 | **本单第二贵、也是最大的单行**。流日志按采集流量计费，常开全量采集会稳定浪费。和 SLS 是同一套网络可观测，不是业务计算 |
| 建议 | ① 确认是否仍要全量 VPC flow ② 能关就关 ③ 必须留则改采样、缩短保存、只采拒绝/异常 ④ SLS 外网读取改内网 endpoint，避免再付外网读 |
| 预估节约 | 关停 ≈ **¥1,055**；采样到 10%–20% ≈ **¥800–¥950**。SLS 再降约 **¥7–¥15** |
| 风险 | 低–中。关了就失去这份网络取证。先问合规 / 排障是否还依赖它 |
| 优先级 | **P0**（金额清楚、不依赖集群利用率） |

---

### 2.3 块存储　¥633

先把概念分开，否则会和 ACK 系统盘重复计数：

| 账单产品 | 计费项 | 这笔在哪 | 是什么 |
|----------|--------|----------|--------|
| **云服务器 ECS** | 系统盘 | 已计入 §2.1 的 ¥3,968 | 节点系统盘 |
| **块存储** | 磁盘容量 / 快照 | **本段 ¥633** | 额外数据盘、ACK PVC/PV、快照 |

| 字段 | 内容 |
|------|------|
| 构成 | 磁盘容量 ¥620 + 快照链 ¥12 |
| 明细 | **100 块整月单价 ¥5.3568**（合计 ¥536）；另外 57 块不足月合计 ¥85 |
| 问题 | 单价和 BI 单里新加坡 40G 小盘完全一致。100 块整月盘 vs 94 台整月节点，数量对得上「几乎每节点一块数据盘」，也对得上「缩容后 PV 没删」。nickname 是 `disk-<uuid>`，像 CSI 动态卷 |
| 建议 | ① 控制台筛 **未挂载 / 未绑定 PVC** ② 确认 StorageClass 是否 Recycle/Delete ③ 缩容节点时同步删 PV ④ 快照链看是否还要 |
| 预估节约 | 未挂载按 70%–100% 释放 ≈ **¥400–¥536**；不足月那 57 块随节点走，不必单独追 |
| 风险 | 低（未挂载）；中（已挂载但看似空，要确认应用数据） |
| 优先级 | **P0**（先清未挂载） |

---

### 2.4 负载均衡　¥621

| 字段 | 内容 |
|------|------|
| 台数 | 23 台 CLB（`lb-t4n*`） |
| 构成 | 实例费 ¥604 + 公网 IP 保有 ¥15 + 容量单位 ¥2 |
| 整月 | **22 台实例费各 ¥27.342**；1 台不足月 ¥2.17 |
| 公网 | `lb-t4ns8b1jyhz8doj30mcze`、`lb-t4nx0k8h4a48shaz71pp6` 各付保有费 ¥7.44 |
| 问题 | **CU 合计只有 ¥2.06**。22 台整月在付实例费，容量几乎没人用。nickname 是 32 位 hex，典型 ACK Ingress / Service 自动开的 CLB：一个服务一台 |
| 建议 | ① 列出每台后面的 Ingress/Service ② 无业务的删 ③ 能共用一台 Ingress CLB 就合并 ④ 两台带公网 IP 的确认是否必须公网；内网 SLB 更便宜 |
| 预估节约 | 22 台收到 6–8 台 ≈ **¥380–¥440**；再去掉不必要的公网保有约 **¥15** |
| 风险 | 中。删错 Ingress 会断对外入口。先对 DNS / 证书 / 业务域名 |
| 优先级 | **P0**（闲置证据已经在账单上：CU≈0） |

23 台清单（按金额，含保有/CU）：

| 实例 | nickname（截断） | 约 ¥ | 备注 |
|------|------------------|------|------|
| `lb-t4ns8b1jyhz8doj30mcze` | `ae4ddd05…` | 34.84 | 实例 + 公网 IP + 少量 CU |
| `lb-t4nx0k8h4a48shaz71pp6` | `ab78b884…` | 34.79 | 同上 |
| `lb-t4nqjjo97eri9nmant3gr` | `a34fc18e…` | 28.39 | CU ¥1.05，本单唯一略有量的 |
| 其余 19 台整月 | 各 `a*` hex | 各 27.34–27.92 | CU 接近 0 |
| `lb-t4nwh32fwt7nbkkbav364` | `a22c1e30…` | 2.17 | 不足月，可能已删或新建 |

---

### 2.5 NAT 网关　¥218

| 字段 | 内容 |
|------|------|
| 实例 | `ngw-t4n034npm655nm4b8hdvo` |
| 构成 | CU ¥129 + 实例费 ¥89 |
| 问题 | 金额不大。节点若继续挂公网 IP，NAT 并不是唯一出口；若按建议去掉节点公网 IP，**部分流量会转到 NAT，CU 可能上升** |
| 建议 | 保留这套 NAT。节点收公网 IP 时，用 NAT CU 换安全面，不要为了 ¥129 先拆 NAT |
| 预估节约 | 不作为本轮目标。治理公网后 CU 可能持平或略升 |
| 风险 | 拆 NAT 会导致无公网节点出不去 |
| 优先级 | **不做**（本轮） |

---

### 2.6 公网流量 CDT　¥97

| 字段 | 内容 |
|------|------|
| EIP 出方向 | `eip-t4nrxabd9ully4adn2brk` **¥49**（3 条 BGP 计费） |
| ECS 固定公网 IP | **¥47 / 370 行 / 161 台 Worker 全覆盖** |
| CLB 公网出方向 | ¥0.05，可忽略 |
| 问题 | 钱少，**结构差**。K8s Worker 不应台台有公网 IP。流量费只是账单上的可见部分，安全暴露面更大 |
| 建议 | ① 节点池关掉「分配公网 IP」② 入口走 Ingress CLB，出口走 NAT ③ EIP 对到具体服务，能并进 SLB 就并 |
| 预估节约 | 流量本身约 **¥47–¥90**。主要收益是安全，不是金额 |
| 风险 | 中。改出口要验证镜像拉取、Webhook、第三方 API |
| 优先级 | **P0（安全） / P1（金额）** |

---

### 2.7 RDS `biopenclaw`　¥89

| 字段 | 内容 |
|------|------|
| 实例 | `rm-t4n810508q0ruz08g`，nickname `biopenclaw` |
| 构成 | 规格 ¥60 + 存储 ¥29 |
| 评测 | OpenClaw / BI 侧小库，规格和存储都正常。不是主战场 |
| 建议 | 看连接数和存储水位；能降档再降。本周不动 |
| 预估节约 | **¥0–¥20** |
| 优先级 | **P2** |

---

### 2.8 Tair `adclaw`　¥36

| 字段 | 内容 |
|------|------|
| 实例 | `r-t4nk9cenxrnd7drinm`，nickname `adclaw` |
| 构成 | 分片规格 ¥31 + 磁盘 ¥5 |
| 评测 | 缓存体量正常 |
| 建议 | 保留 |
| 预估节约 | 忽略 |
| 优先级 | **P2** |

---

### 2.9 OSS / SLS / EIP 保有　¥42

| 对象 | 约 ¥ | 评测 |
|------|------|------|
| OSS 新加坡标准存储容量 | 16 | 很小，生命周期可开但收益有限 |
| OSS 外网流出 + 请求 | 3 | 正常 |
| SLS `clawhub-network-flowlog` | 15 | 跟 §2.2 流日志一套；外网读 ¥7.5 可改内网 |
| EIP 保有 `eip-t4nrxabd9ully4adn2brk` | 7 | 和 CDT 出流量是同一只 EIP |

建议：SLS 随流日志一起处理。OSS / EIP 保有不单独立项。

---

## 3. 优化方案总表（按浪费金额 × 风险）

| 优先级 | 动作 | 预估月节约 | 证据门槛 | 风险 |
|--------|------|------------|----------|------|
| P0 | VPC 流日志关停或采样 | 800–1,055 | 确认合规/排障是否还要全量 | 低 |
| P0 | 释放未挂载 40G 盘 | 400–536 | 控制台「未挂载」 | 低 |
| P0 | 合并/删除闲置 CLB（22 → 6–8） | 380–440 | 账单 CU≈0 + Ingress 清单 | 中 |
| P0 | Worker 去掉公网 IP | ~50 + 安全面 | 节点池配置 | 中 |
| P1 | ACK 常驻节点缩 15%–20% | 4,300–5,900 | CPU/内存水位、空节点 | 中高 |
| P1 | ACK 常驻节点缩 30% | ~8,700 | 同上，需更长观察 | 中高 |
| P1 | 系统盘从 200G+ 收到 80G | 1,000–1,800 | 磁盘占用 | 中 |
| P1 | 压节点伸缩噪声 | ~1,000 | HPA / 节点池事件 | 中 |
| P2 | RDS / Redis 降档 | 0–20 | 连接与内存水位 | 低 |

**保守合计（P0 + 常驻缩 15%）≈ ¥6,200（约 17%）**  
**进取合计（P0 + 常驻缩 30% + 系统盘）≈ ¥10,500（约 29%）**

对照目标：

- 15%（¥5,400）：P0 三项 + 常驻少缩约 12–14 台就能到  
- 30%（¥10,700）：必须动常驻节点池，只清流日志和磁盘到不了

---

## 4. 建议执行顺序

```text
第 1 周（P0，账单上已能确认）
  1. 流日志：关停或改采样
  2. 块存储：列出未挂载 d-t4n*，确认后释放
  3. CLB：按 Ingress/Service 对账，CU≈0 且无域名的删或合并
  4. 节点池：新扩节点不再分配公网 IP；存量按批次摘公网

第 2–3 周（P1，要监控）
  5. 导出节点数、规格、request/limit、实际水位
  6. 先回收空节点 / 超配，常驻目标 -15%
  7. 系统盘占用与容器日志；能缩盘再缩
  8. 看 HPA 是否无意义打出节点

第 4 周及以后
  9. 水位稳定后再评估 -20% / -30%
  10. RDS / Redis 有空再看，不挡主线
```

每做完一项，用账单对应行核对下降幅度。节点缩容观察窗口生产 ≥ 24h。

---

## 5. 还缺的证据（补了才能把「预估」改成「已确认」）

账单已经能排序。下列材料用来 **防误砍**：

1. ACK `ca171f34…` 节点池列表：规格、期望台数、实际台数、CPU/内存 P95  
2. 94 台整月节点上的 request 总和 vs 可分配资源（是否超配）  
3. 系统盘真实占用（是不是 Docker 日志 / overlay 撑大）  
4. 157 块 `d-t4n*` 里有多少 **未挂载**、多少仍被 PVC 引用  
5. 23 台 CLB 对应的 Ingress / Service / 域名，两台公网 CLB 后面是谁  
6. 流日志 `fl-t4nuu6j1kygvp0lgrm1am` 是否合规或排障仍在用  
7. 节点公网 IP 上跑的是 kube 组件、业务、还是误开  
8. NAT / EIP 实际出网是镜像、模型调用、还是应用 API

没有 1/2，不要对常驻节点池下手；P0 四项可以先做。

---

## 6. 口径说明

- 金额为税前，未含折扣后实付差异；若财务口径是折后，按同一比例缩放目标即可。  
- ACK Worker 按集群 nickname 汇总，评估按集群，不要按单 InstanceID 理解成一台天价机器。  
- 161 是「本账期出现过的唯一 Worker」，不是「现在线上有 161 台」。线上常驻更接近 **94 台整月 + 若干弹性**。  
- 系统盘在 ECS 计费项里；块存储是额外盘。两笔不要加总后再说「磁盘 ¥4,600」当同一件事。  
- 小盘数量按单价 ¥5.3568 计 100 块整月，精确数以块存储控制台为准。  
- 本单没有 EMR / Kafka / Flink / VPN，不能套 BI 那张降本清单。  
- 账号列 `1.34E+15` = `1340062516391428`，和流日志、SLS 行里的账号一致。

---

**修订**：2026-08-19，按当前 `adclaw-diangao-ali` 账单明细出具。数字随下一期账单更新。
